#!/bin/sh
##
##  Replace system's ldconfig (in PATH only) with this one to
##  prevent timestamp updating in /data by libtool.
##
##  Note that /usr/local/bin should be first in PATH otherwise
##  this script doesn't change everything.
##

true
